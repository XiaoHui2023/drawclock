# 路由归因统计与行距预算

- status: done
- created: 2026-09-02 10:14 +08:00
- updated: 2026-09-02 12:29 +08:00
- scene: 通用零入度源显示副本与逐边/逐源路由归因

## 当前事实

- 用户重新明确通用目标：零入度源同时服务同行节点和上方约四行节点时，也必须按最终几何评估显示副本，不能把“远距”限定为某个命名场景或只有极端空带；三个以上独立远距消费带也不能被任意截断为两个锚点。
- 已撤销上一轮“分散源复制闭环完成”的范围声明，并把问题归类为 `coverage_escape + claim_escape`：旧测试覆盖了两锚点边界，却把“最多两个”误当成通用安全策略。
- 新增失败基线前先验证相邻成功语义：同行与上方四行的两点设施样本正确拆为两个分区；四个各相隔四行的消费带却只返回 `[[1], [2, 3, 4]]`，预期为四个独立分区。聚焦结果 1 通过、1 失败，pytest 0.60 秒，含进程启动 1.969 秒。
- Find Skills 用四组布局、正交路由、源复制和设施选址查询完成，未找到比现有 `clock-tree-layout` 更专门且可信的可安装 skill，因此未安装第三方 skill。联网资料覆盖 yFiles 官方总线/edge grouping、ELK 官方 edge sections 与 hyperedge 限制、设施选址、rectilinear Steiner routing、vertex splitting 论文、ELK issue、Graphviz/Stack Overflow/论坛反例。
- 当前结构性候选是：以几何行距归一化的固定开设成本求任意数量连续一维设施分区；别名数量由数据和成本决定，不再固定为二；面积降为交叉、折点和线长之后的次级目标；补齐逐边/逐源完整曼哈顿长度、横纵长度、折点及交叉归因。
- 下一步先校验专项合同，再修改唯一生产所有者和独立 Oracle；随后覆盖近距不拆、同行+四行、三/四远簇、多源、非 root 禁止、重命名/顺序扰动、连续扇出不滥拆、4096 压力和打包消费。
- 已将专项合同保存到项目布局 skill，绑定能力边界、四类量化指标、双基线、故障注入、三轮预算、回退点和父链门禁；合同结构校验是进入生产修改前的下一道门。
- 专项合同已通过结构校验。第一轮生产修改删除二锚点硬上限，让固定成本设施动态规划返回任意数量的几何合理连续分区；源副本接受门继续禁止拓扑、障碍、异网重合、交叉和折点退化，但面积降为次级记录，不再覆盖可读性收益。
- 生产统计已扩展为逐边完整曼哈顿/横纵长度、最长竖段、效率比、折点和线段数，并按逻辑源聚合锚点、长度、折点和最大单边长度；全局汇总同时记录交叉边对、不同交叉点、源引发交叉点、长度和折点。下一步由独立 Oracle 复算相同量纲。
- 独立 `layout_quality` 已从最终端口与 waypoint 重新规范化每条边并复算同一统计合同，不读取生产统计结果；后续 equality 测试负责发现任一字段漂移。
- 项目布局与成图 skills 已同步通用规则：同行源也可复制到四行外的远簇，锚点数由设施成本决定而非固定为二，面积不得覆盖交叉/折点/线长优先级；统计合同扩展到逐边和逐源完整长度与折点归因。
- 第一轮聚焦为 7 通过、1 失败（pytest 0.66 秒，整段 2.181 秒）。失败样本故意含一条非正交直线，独立 Oracle 的旧全局长度仍把该无效段按曼哈顿代理计入，而新逐边合同只统计可见正交段，形成 449.843 与 374.921 双口径；已改为全局值严格汇总逐边规范化值，非正交错误继续由独立硬门单独拒绝。
- 口径修复后原聚焦集合 8/8 通过（pytest 0.42 秒，整段 1.878 秒）。一次独立四带集成探针让同一零入度源形成四个锚点、节省 3168 px、面积不增，证明生产别名创建链已能消费任意分区，而非只有纯函数返回值变化。
- 已把四带集成探针固化为正式回归，并加强统计断言：全局边数/长度必须等于逐边求和，逻辑源长度必须等于其出边求和，生产报告仍须与独立 Oracle 整体相等。
- 扩展聚焦为 8 通过、1 失败（pytest 0.66 秒，整段 2.038 秒）；唯一差异是十进制报告值 841.72 与 Python 浮点逐项求和 841.7199999999999。断言改用固定 0.001 px 的表示容差，不改变生产量纲、算法阈值或视觉验收。
- 表示容差修复后专项聚焦 9/9 通过（pytest 0.47 秒，整段 1.885 秒）。同时移除拒绝诊断里的伪 `area` blocker：面积现在只记录 delta，不再被错误表述成会阻止清晰源副本的硬条件。
- 新增邻近反例覆盖：输入顺序和任意 ID 不影响连续分区；四个密集相邻行保持一个源；相同远距扇出放到有父节点的中间器件时不得产生任何别名。它们与四远带正例共用同一几何构造，不读取器件名决定资格。
- 扩展专项首次为 11 通过、1 失败（pytest 0.70 秒，整段 2.108 秒）。失败用例已正确禁止中间器件别名，但测试错误要求人为拉成长路线的坏图整体通过；现改为同时断言“零非法别名”和“完整质量门拒绝坏图”，收窄声明范围而不放宽门槛。
- 领域回归 179 通过、1 失败（pytest 85.91 秒，整段 87.334 秒）：四个复用源/24-clock 场景产生 41 个显示副本。根因是旧设施成本把共享竖干线误算为所有消费者到中位轴的 L1 和，随扇出数放大并过度奖励复制；改为每簇只计算一次 `max(axis)-min(axis)` 干线跨度，再加几何开设成本。该模型直接对应一源多目标 rectilinear bus，不靠恢复固定副本上限。
- 干线跨度模型复跑后仍为 12 通过、1 失败（整段 2.187 秒）。统计显示 41 个副本对应四源共 48 条出边，消除 276 个交叉并节省 37,197.53 px，且无未使用或可合并冗余；旧断言“全图副本不超过 4”是固定数量代理指标，和通用效果优先目标冲突。已替换为每锚点有边、锚点不超过出边、无设施成本冗余且必须产生交叉/线长收益的结构门。
- 替换代理门后专项 13/13 通过（pytest 0.63 秒，整段 2.149 秒）；布局、独立几何、属性语料、SVG 跨线和可见端口领域回归 180/180 通过（pytest 87.65 秒，整段 89.380 秒）。
- 用户根 `clock-tree-layout` 已回写跨项目耐久结论：共享干线按跨度只计一次、固定副本上限属于反例、任意设施分区必须受锚点使用性和可合并性门约束、逐边/逐源统计需覆盖完整长度与折点。来源页记录了本轮官方、论文、社区和 Find Skills 状态及一次 PDF 正文读取失败。
- 首次全量门 376 通过、6 失败（pytest 88.83 秒，整段 90.930 秒）。一项真实失败是新增专项 JSON 未从项目 skill 索引链接，已补入口；其余五项均为 Edge `--dump-dom` 返回空字符串，连成功/故障对照都未产生 DOM 证据，按工具链阻塞单独诊断，不能提升为几何通过或产品失败。
- 浏览器探针失败的共同前置条件是系统已有多个 Edge 常驻进程；初步假设为默认配置复用，但独立 `--user-data-dir` 仍以退出码 0 返回空 stdout，故该假设已被否定，不能声称修复。测试现把“二进制存在”和“能完成最小 DOM 往返”分成两层能力探针：只有后者通过才运行浏览器几何 Oracle，否则明确 skip；这避免把外部浏览器服务失效误报成五项产品几何回归，同时保留恢复工具后自动重启强门的能力。
- 第二次全量门为 375 通过、2 个性能失败、5 个浏览器能力 skip（pytest 134.61 秒）：2048/4096 分别为 12.146/42.689 秒，拓扑和质量语义均通过。隔离复跑仍为 11.181/35.736 秒，确认不是单次抖动。2048 cProfile 显示新增 `intervening_rows` 对每边线性扫描所有视觉行，单个生成器执行约 830 万次并形成 `O(E*R)`；现用已排序行坐标的 `bisect_right/bisect_left` 精确计算开区间内行数，降为 `O(E log R)`，不改变统计定义或阈值。
- 二分统计后 2048 恢复预算内，4096 仍为 37.362 秒。主热点进一步定位到任意源设施分区的 `O(k^2)` 动态规划。对“每簇跨度 + 固定开设成本”的当前精确目标，任一相邻间隙切分的目标差恒为 `fixed_cost-gap`，与其他切点独立；因此改为对排序消费带在 `gap > fixed_cost` 处切分的闭式最优解，复杂度 `O(k log k)`（排序占主导），相等时保持少锚点。新增 1024 组小状态穷举 Oracle 比较目标值、锚点数和切点，防止闭式推导或以后成本修改漂移。
- 闭式分区后 4096 为 35.936 秒，仍超预算。复剖析显示五次 `assess_layout` 合计占主要耗时，候选源此前每次深拷贝全图，而普通候选评估还构造了只供最终报告使用的逐边字典。候选现采用 copy-on-write：只复制待移动源、其出边以及顶层列表，其余不可变布局对象共享；初始接受图不再无故深拷贝。逐边详细指标仅在最终 `include_routing_statistics=True` 时构造，所有候选硬门仍完整计算节点/线碰撞、异网重叠、交叉、折点和总线长。
- copy-on-write 后 12 项专项语义通过，但 4096 仍为 35.994 秒，证明深拷贝不是主导瓶颈。最终交叉归因原先在每个可见交叉点对共享干线的所有逻辑边逐一更新集合；现先按去重后的可见几何干线聚合交叉点和事故数，再向该干线拥有者一次分发，并对可能的同边自交做扣减。这样不改变逐边/逐源统计，却避免 `owner_count * crossing_count` 次重复集合更新；生产与独立 Oracle 的整体相等门负责防止归因漂移。
- 分段计时的 4096 样本总计 32.178 秒：基础布局 14.618 秒、源副本 9.203 秒、五次完整 `assess_layout` 累计 15.234 秒。两个候选源中一个已由 `edge-node` 硬门确定拒绝，旧流程仍继续计算其交叉与异网重叠。评估器现支持通用几何短路：节点重叠或线穿节点一旦超过当前接受图就立即返回明确 blocker；未越过该前门的候选仍执行完整交叉、重叠、折点和线长评估，门槛与最终报告不变。
- 几何短路后 4096 降为 34.721 秒。源副本阶段对最终接受图已有完整硬指标，最终详细统计旧实现又重算节点桶、节点重叠、线穿节点、异网共线和方向。现仅在生产内部把该次接受评估传给紧随其后的最终统计扫描复用；最终扫描仍从最终坐标重算交叉、折点、总长及逐边/逐源归因，公开报告不暴露缓存，独立 Oracle 仍重算全部硬指标。
- 无干扰分段复测为 26.223 秒，但连续两轮压力组一轮通过（总计 36.77 秒），另一轮 4096 为 32.311 秒，证实主机墙钟波动仍会击穿门。`exclusive upstream chain` 阶段在该图报告 0 moves/0 blockers，却先深拷贝、全评估和生成可见签名，空耗约 3 秒。现加入与规模无关的必要条件预检：仅当存在单出边、两折点、纵向错位且独占上游链可追溯到根的边时才进入昂贵闭环；有候选时原完整门禁不变。
- 预检后链轴正反例与压力门连续通过两轮：首轮 6 项（含 3 个规模）32.72 秒，次轮 3 个规模 37.82 秒，各单例均满足 5/10/30 秒预算。全量门为 378 通过、5 个明确浏览器能力 skip、0 失败（pytest 98.72 秒）；浏览器 skip 不计为几何通过，恢复 DOM 能力后会自动重新启用。
- 上传前审查识别出两份本轮 cProfile 临时文件；它们不属于源码或质量证据制品，已删除并用 `*.prof` 通用规则阻止后续误提交。
- 本地 Windows `tools/pack.bat` 在约 92.4 秒内生成 `drawclock-1.0.0-windows.zip`。从系统临时目录全新解压后，包内七项 skill、冻结程序完整样例矩阵及全新 venv 的 `python -I -S` 零依赖源码部署全部通过（约 41.1 秒）；证明当前源码、runtime、多文件器件库、示例和 skills 均进入归档且可消费。
- 上传前 changelog 对照发现最新决议仍保留“每源最多两个锚点”的旧限制，与本轮通用目标冲突；已暂停上传并改写为共享干线跨度成本、任意精确间隙切分、结构接受门、完整统计和性能闭环，防止旧决议在后续维护中复活。
- 功能提交 `facfb310c6ca1ce51108e89f36d03ad413bb9528` 已推送到 `origin/main`。Release run `33590442298` 的 Ubuntu 16.04 构建与 publish job 均成功；滚动 `v1.0.0`、远端 main 和本地 HEAD 精确同点。唯一资产 `drawclock-1.0.0-linux.tar.gz` 为 51,913,217 bytes，下载 SHA-256 `91BE98B1E886213CE34166975E1863013A1C663DC119808A10B8B09C97084AF5` 与 GitHub digest 一致；Docker 解压消费仍待执行。
- 下载资产在全新 Python 3.10 Docker 容器中通过冻结程序完整工作流和 `python -I -S` 源码部署（约 40.6 秒）。通用源副本、完整路由统计、反例/性质/规模测试、本地包、CI、tag、摘要和远端下载消费全部闭环；记录置为 done，随后滚动发布本记录提交，使最终 tag 覆盖完整证据。

- 原实现只报告总体交叉点和根源交叉点总数，无法定位具体高风险边或逻辑源。
- 原分散根源拆分以水平服务距离参与固定成本，只有很大的垂直空带才容易形成候选，不能表达“稍远就优化”的可视行语义。
- 已开始加入每边、每逻辑源的交叉点、相交边、垂直跨度、夹行数统计，并将候选边界改为几何归一化的三行预算与实际夹行数双判据。
- 首轮聚焦测试为 1 通过、5 失败；失败均由 `src/auto_layout.py` 漏导入 `statistics.median` 引起，错误为 `NameError: name 'median' is not defined`。该失败发生在布局评估入口，尚未形成错误制品。
- 已补齐 `statistics.median` 导入并移除未使用的键映射，下一步复跑同一测试确认恢复。
- 第二轮聚焦测试为 3 通过、3 失败。诊断发现直接去重节点中心会把同一视觉行内约 8 px 的器件/端口偏差当成独立行，造成过度拆分；现改为先按中位器件高度聚类成视觉行带，再以行带中心和典型间距计算预算。
- 行带修复后聚焦测试 6/6 通过。现将交叉归因改为线性空间统计：每边记录不同交叉点和交叉对事件数，每源聚合；避免为了列举所有相交边而在大扇出上物化笛卡尔积。独立 Oracle 已按可见几何另行复算同一合同。
- 已增加两个强负载判据：手工注入一处异网正交交叉，要求两条边与两个源都各归因一次；把分散消费者仅下移约四个典型行距，要求无需“超远”间隔就能形成并接受一个安全渲染副本。
- 首个中距离样本使用 400 px 总位移，但扣除原有消费者排布后相邻带净间隔为 288 px，未超过实测三行预算 309 px，因此正确未触发；边界改为 425 px（净间隔略高于预算），保留“等于/以内不拆、刚超过即候选”的严格语义。
- 聚焦门禁现为 9/9 通过，包含 1024 规模硬几何检查。公开项目 skills、设计笔记、质量合同与用户根部 `clock-tree-layout` 已同步三行预算、视觉行带、逐边/逐源归因、独立 Oracle 和线性空间统计规则。
- Find Skills 分别检索 graph drawing crossing metrics、layered graph layout optimization、orthogonal routing quality，命令均成功但没有返回专门覆盖分层正交路由质量的可用 skill，因此未安装泛化网页布局/绘图 skill，继续使用现有专用 `clock-tree-layout`。官方 ELK 分层阶段、交叉最小化、固定端口正交路由以及分层图 edge span 定义已作为方法基准。
- 项目七项 skill 与五件套校验均通过。用户根部 skill 首次校验误用非系统路径报 `No such file or directory`；改用正确系统路径后又因 Windows 默认 GBK 读取 UTF-8 报 `UnicodeDecodeError`，设置 `PYTHONUTF8=1` 后恢复并通过 `Skill is valid!`，对交付无残留影响。
- 首轮全量测试为 359 通过、17 失败（111.21 秒）。15 项质量失败来自独立副本 Oracle 仍使用旧水平距离固定成本，与生产三行预算不一致；2048/4096 两项性能失败分别为 10.207/37.880 秒。现把 Oracle 固定成本统一为三倍独立测得行距，并将详细逐边归因从所有临时候选移到最终布局一次生成；候选仍计算原有聚合硬指标。
- 详细统计开关重构的首笔修改一度保留了提前 `return`，静态查看立即发现不可达附加逻辑；同轮修正为先构造 `report` 再条件附加统计，尚未作为通过证据。
- 失败集合复跑为 1 通过、15 失败；共同错误是最终 `routing_statistics` 键缺失。检查发现通用文本补丁误把 `_place_nodes` 的 `return` 改成局部 `report`，却未命中 `assess_layout` 的目标返回。现按相邻函数语境精确恢复 `_place_nodes` 返回并修改评估返回；后续结构性替换必须在修改后读取命中区段，不能只依赖编译。
- 精确修复后受影响集合 13 通过、3 失败。两项列布局失败来自聚类后 37.499 px 行距小于典型器件高度，普通 126 px 分支被拆成三个锚点；多源样本虽质量通过但 4 个逻辑源膨胀为 48 个物理 y 位置。现将行距下界设为中位器件高度，并限制每个逻辑源一次最多两个锚点、选择最佳连续切分；原多源断言改为只比较四个原始逻辑源位置，同时限制副本总数不超过四。
- 行距/双锚点修复后的受影响质量集合 16/16 通过（45.70 秒）；1024/2048/4096 规模性能组 3/3 通过（合计 36.04 秒），各自均低于既有 5/10/30 秒预算。已移除 Oracle 中随旧成本定义遗留的未使用水平距离变量，准备全量复验。
- 全量回归 376/376 通过（104.21 秒）；`compileall`、`git diff --check`、项目七项 skill、五件套及用户根部专题 skill 均通过。代码与文档进入提交和滚动 Release 闭环。
- 功能提交 `5984b69` 已推送；GitHub Release workflow `33583777618` 成功，`v1.0.0` 首轮 tag 精确指向该提交。远端附件 `drawclock-1.0.0-linux.tar.gz` 为 51,905,905 bytes，下载 SHA-256 为 `DCF81D465D1986AAACFB58FA448FD1E68E62BF35C2D09D1EA53A53DC94855D32`。
- 本机缺少 GitHub CLI，调用 `gh` 返回 `CommandNotFoundException`；改用 GitHub 官方 REST API 完成有界轮询、Release 元数据与 tag 核验，不影响构建。首次 `Invoke-WebRequest` 下载未在 30 秒内返回，改用带 180 秒上限和 3 次重试的 `curl.exe`，8 秒下载成功。
- 下载附件已在 Ubuntu 22.04 Docker 中解压，以隔离 `PATH=/nonexistent` 直接运行冻结 `drawclock`，使用包内线性配置和器件库生成 5,867-byte SVG；目标平台消费门通过。工作记录完成后再发布一次，使最终 tag 覆盖本记录提交。

## 质量闭环

- 修复导入后重跑同一聚焦测试。
- 用独立 `tests/layout_quality.py` 复算归因统计，避免生产实现自证。
- 增加三行边界正反例、交叉故障注入、同网共享干线非交叉例，并执行全量测试和发布门禁。
- 只有候选同时不恶化节点重叠、边穿节点、方向、异网重叠、交叉、折点和面积时才接受源渲染副本。
