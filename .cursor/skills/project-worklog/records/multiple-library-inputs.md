# 多器件库输入与合并

- status: done
- created: 2026-08-25 14:03 +08:00
- updated: 2026-08-25 15:45 +08:00
- scene: 多器件库输入与合并

## 当前状态

- CLI 只接受一个器件库文件。
- 形状、端口和连接键缓存都以单个路径字符串为键。
- 合并必须发生在器件库解析边界，不能在布局各调用点分别拼接。
- 已在器件库解析边界加入文件/目录展开、递归 XML 扫描、稳定去重、合并和重复 title 拒绝；CLI 已接受重复多值 `-l`。
- 下游类型标注和正负测试尚未同步。
- 下游类型已统一为多库来源；聚焦测试 42/44 通过，两个失败来自新测试误读报告字段和测试器件集合不完整，产品合并路径未报错。
- 修正测试输入后 43/44 通过；剩余失败是测试忽略 SVG 标准 XML 声明，实际混合多库 CLI 已成功生成 SVG。
- 修正 SVG 断言后聚焦测试 44 项全部通过；覆盖递归目录、多个目录、显式文件、重复 `-l`、路径去重、无效路径和重复 title。
- CLI 和用户专档已说明多路径、递归扫描、稳定合并与同名冲突规则；下一轮优化入口处规范化，避免布局过程中反复扫描目录。
- 两个布局入口和配置校验入口已先规范化为确定的文件元组；后续端口与属性缓存复用该键，不在每个节点或边上重复递归扫描目录。
- 入口规范化后的聚焦回归 44 项通过，未破坏单文件兼容、混合输入与目录递归行为。
- 全量 318 项中 316 项通过；2048 与 4096 压力预算分别为 10.419/10 秒、32.780/30 秒。根因候选是已规范化文件元组仍在每次端口查询中重复执行路径存在、解析与 resolve，需把路径展开本身缓存。
- 路径来源元组现以 LRU 缓存完整展开结果，端口与节点查询复用同一键。压力门复跑 3/3 通过：1024 为 2.31 秒、2048 为 7.00 秒、4096 为 25.31 秒；总计 34.98 秒。
- 最终解压包的冻结门直接使用重复 `-l` 加显式 XML 和嵌套目录，成功生成节点与边数匹配的 SVG；等待远程 Release 下载复验。
- 提交前逐文件 diff 审查发现 `library_ports.py` 在类型迁移后残留未使用的 `Path` 导入；删除后执行最终静态复查。
- 残留导入删除后，源码/工具/测试 compileall、44 项聚焦回归和 `git diff --check` 均通过；pytest 7.96 秒，整组 10.189 秒。
- 功能提交 `e2eb8cc` 的远端 Linux 包在隔离容器中通过混合器件库冻结门；目标收敛。

## 下一步

- 无。
