# 自由源分层、逐元素统计与覆盖闭环

- status: active
- created: 2026-09-02 14:42 +08:00
- updated: 2026-09-03 05:00 +08:00
- scene: 自由源分层、逐节点/逐边统计与特性覆盖闭环

## 当前声明

- 04:35 障碍命中纯查询缓存已实现；4096 同一硬门连续三轮为 21.25/21.15/20.93 秒，含 pytest 进程总耗时 22.684/22.450/22.244 秒，较 30 秒预算保留约 29% 余量。
- 04:37 跨项目自由布局专题补记缓存正确键、不可变值、全量负载与连续运行余量；不包含项目路径、样例名或私人信息。
- 04:40 修复后的最终本地质量门为 400 PASS / 5 条件 SKIP，pytest 94.53 秒，含 compileall、项目 skill 与 21 特性/12 交互/31 场景账本共 96.698 秒。
- 04:50 Windows clean pack 98.253 秒成功；EXE 8,159,216 B，ZIP 37,176,305 B。PyInstaller 警告仅为 Windows 上不可用的 POSIX/Jython 条件模块，继续由 frozen 功能 smoke 判断实际缺失。
- 05:00 全新目录冻结与无依赖源码消费 35.679 秒全部通过；EXE SHA-256 `25B69DA7BBEC32893C925368CE2BE0D24CED622E3ACF948E060FC12A2E4CEE44`，ZIP SHA-256 `8BE7CB6035EE385BB26F2B990B1321B46ED611C6A7EF650D428F4A6283EEAC86`。
- 04:29 全量门捕获 4096 在整套负载下 30.526 秒，未放宽 30 秒门；剖析定位同一 edge/points 障碍查询在可见图扩展、trunk 过滤和评分间重复执行，下一笔改动仅缓存该纯查询。
- 04:06 三阶段已增加仅内部使用的 `accepted_assessment/include_assessment` 传递面；生产流水线逐段 pop 后传递，最终统计复用锚点阶段的精确版本。公开 CLI 与报告不暴露内部 assessment。
- 04:01 1024 profile（插桩 6.547 秒）显示 `assess_layout` 9 次累计 3.419 秒、占 52%；新签名未进前 28 热点。主分区、局部设施、锚点和最终阶段将显式传递已接受图 assessment，仅候选改变才重算，目标从 9 次降到 6 次。
- 03:53 4096 阶段计时总 37.734 秒：主分区 7.043、局部设施 4.312、锚点 5.887 秒。跨阶段热点是同一静态 `(name,label,width)` 数百次重复执行标签 HTML/CSS 正则宽度估算；该纯函数与 x/y 无关，加入缓存，VisualBox 坐标仍实时计算。
- 03:44 候选全图可见 overlap 绝对判空使 4096 整组动作受无关旧重叠阻塞，耗时退化到 37.819 秒。修正影响域：节点 overlap 只检查移动/新增 source 焦点盒对全体节点；incident edge hit 仍检查受影响边对全体节点。
- 03:36 删除基线反向索引后 4096 为 31.355 秒，仍未通过 30 秒门；19 三门 3/3。可见节点重叠与边穿可见盒也是绝对空集不变量，下一步删除已接受图的重复可见签名，只计算候选并要求空集。
- 03:29 反向穿越门以候选命中必须为空为合同，删除已接受图的重复未改边索引；局部设施阶段同时删除未使用的基线计算。修复 INDEX analysis 行缺失更新时间列导致的五件套门失败。
- 03:22 区间索引假阳性 `e116 -> n8` 根因为水平端点 y 存在约 1e-13 浮差：索引用容差判水平，旧矩形函数用严格等号判斜线并保守返回命中。入索引时将容差内固定轴规范为两端完全相等，排序轴同步使用规范值，再执行精确矩形求交。
- 03:15 焦点盒反向穿越从二维像素桶改为正交区间索引：垂直段按 x、水平段按 y 排序，焦点盒以坐标范围二分候选后做精确矩形求交；非正交异常段保守逐盒检查。长纵线成本不再随跨越像素桶数量增长。
- 03:06 可见穿越影响域拆分：受影响 incident edges 继续对全体可见节点检查；未改边只通过空间桶查询移动/新增 source 焦点盒。锚点移动比较候选/基线反向命中集合，新增设施要求反向命中为空；全图可见节点重叠仍由签名完整计算。
- 02:57 撤销选列前的 L1 分区合并：设施是否可合并取决于最终列；异列设施可能分别靠近消费者且避障，不能只按 y 合并。细候选先联合求列/行/路线，最终同列设施才由全根 L1 开启成本硬签名拒绝。
- 02:50 将候选边界与设施经济性分离：细几何行距生成候选分区，全根视觉行带开启成本驱动相邻 L1 中位服务分区的栈式合并，直到每个设施都能覆盖自己的开启成本。避免直接提高 gap 阈值漏掉组合收益，也避免 seed8 的两设施可合并。
- 02:41 统一设施开启成本：所有节点中心先按中位器件高度聚合视觉行带，再以行带中心差和两倍中位高度得到动态行距，开启成本为 3 行。局部分区、锚点冗余门与独立 Oracle 现在共享定义但保持独立实现；seed8 的 55/107 px 合同分裂被移除。
- 02:34 原始路线硬签名扩展为首尾水平、逐段正交及零/微段；新增全根设施冗余签名，以同列相邻设施合并后的 L1 服务成本与几何开启成本判断，候选不得新增可合并设施。设施身份用 cell_id 对，不依赖逻辑名称模式。
- 02:25 候选 source 图标会成为所有既有边的新障碍，因此可见穿越签名恢复全边扫描；只扫 incident edges 会漏掉“其它边穿过新增节点”。端点签名仍限定受影响边。微/零线段改用未简化端点与航点序列，长度小于 1 px 即进入硬签名，避免简化器先删掉证据。
- 02:18 锚点后移与局部设施接受门均比较受影响边的候选/已接受可见穿越集合和端点签名；新增标签盒穿越、可见节点重叠、源/目标净空或微线段即拒绝，并记录对应 blocker。软质量向量仅在这些硬签名不劣化时比较。
- 02:13 可见签名支持只扫描受影响边，同时仍对全体节点建立标签扩展盒；新增独立于测试 Oracle 的产品内端点签名，覆盖源/目标水平引线净空和 0.01–1 px 微线段。
- 02:08 几何短路首个补丁因同形 `candidate_report` 代码先命中锚点阶段；视读后已补到局部设施阶段，两处均保留。全量 405 项得到 395 passed / 5 failed / 5 skipped；失败分类为新增可见标签/器件穿越、端点净空不足、微线段及伴随冗余设施。下一步对受影响边加入可见盒、端点净空和微线段硬签名，候选不得新增签名。
- 01:56 4096 实测 30.062 秒，仍未通过 30 秒门。局部设施候选加入几何短路：节点重叠或边穿器件一旦比已接受图更差，立即拒绝，不再计算交叉、折点、长度等软指标；零硬错误候选仍执行完整质量向量。
- 01:48 4096 阶段计时：总 30.430 秒，分区 4.906、局部设施 4.152、锚点列 4.867 秒。锚点 scope 的顶点/边索引改为根级缓存、仅在接受候选后刷新，避免每个已收敛单锚点 scope 重建全图字典；组内行避障同样改为固定列盒缓存与动态设施盒。
- 01:39 最近可行 y 从“所有边界 × 所有禁止区间”的二次验证改为排序合并区间后的精确查询；目标在可行域直接返回，落入合并区间只比较合法左右边界。语义保持最近非负可行位置，复杂度由 O(k²) 降为 O(k log k)。
- 01:31 2048 阶段计时：总 13.008 秒，分区 1.429、局部设施 5.584、锚点列 2.032 秒；两个零入度根各 258 路。局部设施候选的行避障改用列相关盒缓存，路线避障改用动态 y 空间桶，只扫描候选列/路线附近器件；新增设施盒单独增量维护。
- 01:22 恢复设施分区阶段的单列线性占用扫描：该阶段只决定 y 分区，x 由后续锚点阶段唯一负责。删除分区阶段未被生产采用的列模式及“每个锚点 × 全体节点”盒重建；保留原有器件/走线占用避让和后续完整质量门。
- 01:12 第二次 1024 profile：锚点阶段仍占 8.077 秒，一个共享根的 126 个设施在整组移动后又触发 126 次无效逐锚点全图复制/障碍索引。增加复制前拓扑可移动性预检；整组已到最迟列时不建立后续候选，只有仍有严格更晚可行列的 scope 才复制与评估。
- 01:02 1024 profile 得到 132,209,397 次调用、44.120 秒采样耗时；锚点后移占 29.659 秒，其中 135 次全图深拷贝占 25.240 秒。修正阶段所有权：单逻辑根的列由拓扑 ALAP 层负责，锚点后移只处理分散源产生的多物理设施；几何候选改为浅复制顶点/边坐标与不可变航点，不递归复制未修改的器件属性。
- 00:48 将通用结论回写用户根 `clock-tree-layout`：物理 source 是显示设施、设施分区只有一个所有者、列/行/路线联合求解、禁止旧航点约束新设施、三种交叉统计语义分离，以及一设施反事实门。项目能力合同同步冻结旧基线与新目标值。
- 00:40 增加完整反事实门：同一 19 号结构禁用局部设施后重新执行后续列优化与全图路由，再与正式结果比较。正式结果必须让三类旧长跨度根都形成多个显示设施，交叉至少减半、源交叉至少降低三分之二、长度至少降低四分之一且拐点不增加；重命名/逆序变形门继续独立运行。
- 00:32 测试捕获接口故障：列候选障碍检测把 `VisualBox` 直接传给只接受四元组的 `_segment_hits_rect`，生成阶段抛出可验证 `TypeError`；已改为显式边界四元组，失败运行没有形成可接受产物。
- 00:29 新设施不再无条件取最右拓扑可行列；对所有可行列计算精确源/目标端口的最少折点正交接入，按“障碍命中数、列靠后程度、偏离目标行距离”排序。正确性优先于近邻列偏好，最终仍须通过完整图支配门。
- 00:22 将逐障碍贪心 y 位移替换为通用一维可行区间求解：把所有水平相关器件投影为禁止的 anchor-top 区间，枚举区间边界并选择距目标端口中位数最近的全局可行位置。该计算同时用于新局部设施与已有设施后移，避免一次躲开一个节点后又撞上另一个节点。
- 00:16 修正分区所有权：第一阶段已形成多个物理锚点的根，不再由局部补充分区器逐锚点二次切割；否则两个独立阶段可能产生全根视角可合并的相邻设施。补充分区只救援仍为单锚点的长跨度根，所有已有别名仍进入后续逐锚点列/行优化。
- 00:11 新设施的行距阈值与独立冗余 Oracle 统一为 3 个几何行距；只在消费者间隔跨越多个普通行时复制，避免把每个相邻消费者都变成一个 source 图标。该阈值来自节点高度、行距与固定设施成本，不依赖样例编号、器件名称或规模。
- 00:05 诊断逐源剩余成本：`source_a/source_c/source_g` 分别跨 32/25/20 行，长度 16056.657/14691.877/9000.212 px，贡献 25/25/20 个交叉点。局部拆分候选已形成，但把移动前的内部航点误当成移动后的约束，使新端口到旧通道的连接撞器件。局部别名现在从精确新端口重新计算最少折点 H-V-H，完整硬门与全图质量向量仍是唯一接受条件。
- 23:58 修正候选空间缺口：根锚点后移不再要求同一逻辑 source 已有多个物理别名；零入度、无显式 `layout_column` 的单锚点低复用 source 也进入“最迟可行列”全图支配判定。单锚点的全体/逐锚点 scope 去重，避免重复评估。

旧的 19 号审美完成声明已撤销。用户可见 SVG 在 source 区域仍有大量交叉；已有统计虽然能逐边、逐源归因，但旧验收没有把“物理 source 别名仍继承逻辑根的早期列”列为拒绝条件，因此 topology/geometry 门通过不再代表布局美观通过。

## 23:18 用户否定与目标重开

- 失败基线：当前提交 `612ed9383fbd2a604c3e58ca4fde6de690fddc6f` 的 19 号 JSON、SVG、生产统计和独立 Oracle 输出。
- 逃逸分类：`oracle_escape`（统计量存在但没有对应硬判据）、`coverage_escape`（缺少“别名位于最迟可行列”的完整反事实）、`ownership_escape`（旧副本阶段只调整 y，物理锚点 x 仍继承原逻辑根列）。
- 新硬需求：每个零入度逻辑 source 的每个显示锚点独立计算最迟可行列与局部行；低复用锚点靠近其下级；同名多个锚点互不强制共列；不得按器件名、样例编号、固定尺寸或节点数补丁。
- 新声明边界：在旧失败样本被新门拒绝、修正版及重命名/逆序/库几何变化通过、最终 SVG 与发行包消费完成前，目标保持 active。

## 23:24 失败基线实测

- 生产生成 193.343 ms，独立几何 Oracle 44.918 ms；190 条逻辑边、74,231.207 px 曼哈顿总长、118 个折点、100 个不同可见交叉点，其中 93 个由根源出边参与。
- 逐源最严重者：`source_a` 40 个交叉点、`source_c` 38 个、`source_g` 33 个。最长问题边跨 32 个视觉行、单段纵线 3,445.953 px。
- 所有生产与独立长度、折点、不同交叉坐标、source 诱发交叉坐标一致；但边对交叉事件生产为 245、独立 `exact_pair_oracle` 为 227，旧“统计相等”门未使用精确边对模式，旧准确性声明撤销。
- `source_b` 的 8 个显示锚点 x 均为 72.78；其它被复制根也各自共用一个 x。当前副本算法只改变 y，这是 source 区域交叉的布局所有权根因。

## 23:31 统计合同修复启动

把交叉拆为 `crossing_pair_intersections`（边对×位置事件）、`distinct_crossed_edge_pairs`（不同边对）和 `distinct_crossing_points`（不同可见坐标）。复杂 19 直接运行生产分组扫描与独立 O(E²) 精确枚举；旧精确 Oracle 把边对去重结果覆盖事件字段的行为被永久负例锁定。

## 23:40 物理锚点列所有权实现

显示副本不再只继承逻辑根 x。每个分区从最终图已有规范列中选择仍严格位于全部所服务目标左侧、且可见轮廓保留 route clearance 的最右列；显式 `layout_column` 仍固定原列。随后在该列用器件/标签/既有路线障碍计算最近可行 y，并为同一锚点的边重建一条共享纵向总线。完整 `assess_layout` 仍可拒绝任何碰撞、异网重叠、方向、交叉或折点退化。

## 23:47 第一轮拒绝与 Oracle 修正

首轮 4 项专项为 2 PASS / 2 FAIL。最迟列使可见交叉点 100→97，但边对事件 245→321、折点 118→126；同时独立 Oracle 因旧的“别名 x 必须相等”和逻辑 rank 同列假设拒绝合法多列别名。撤销该轮候选。Oracle 改为位置与身份分离；多锚点零入度节点不参加普通 rank 同列检查。路由恢复既有航点形状并只对新端口位置做前向可行裁剪，使旧路线成为真实回退而非重新画一张更差的所谓基线。

第二轮结构测试 4/4 PASS，但最终量化为 164 个可见交叉点、157 个 source 交叉点、146 个折点，明显劣于冻结基线。原因是预放置阶段把旧边段加入 source 障碍，锚点被推离消费者且多数复制候选被拒绝。该方案撤销；source 坐标只使用当前节点/标签和同批锚点占用，旧边由随后重路由及最终 edge-node 门处理。

## 00:03 阶段所有权拆分

设施分区固定使用继承列生成已验证副本；新的锚点列阶段只消费最终副本—逻辑边分配。它先尝试整根全部锚点，再尝试单锚点；候选列来自当前规范列并受全部服务目标前向/可见间距约束，y 由目标端口中位轴和该列节点障碍求得，同一锚点共享一个首个纵向通道。只有 `(source 交叉点、全图交叉点、边对事件、折点、长度、面积)` 严格变好且硬几何不退化才接受。

首轮阶段拆分专项 3 PASS / 1 FAIL；优化候选已到 69 个可见交叉点、114 个折点。唯一差异是生产总长度直接累加未舍入段得到 68,659.908 px，独立 Oracle 汇总逐边三位值为 68,659.907 px。合同统一为“总计等于公开逐边值之和”，不调整坐标或容差。

## 00:20 累积长总线候选

剩余交叉集中于仍为单锚点且跨 20～32 行的根；旧三行固定成本只观察相邻 gap，无法看见多个中等 gap 的累计跨线和交叉。新增一行局部连续带候选，分区只负责提出显示别名；实际接受仍要求完整图在 source 交叉、全图交叉、边对事件、折点、长度、面积的有序向量上严格改善且所有硬几何不退化。不同 x 的锚点不再被 y-only 设施 Oracle 误判为可合并，因为合并会丢失独立列这一决策变量。

首次 5 项命令因一个不存在的旧测试节点而 0 项执行；更正后 4 个核心门通过，冗余副本故障注入在制造 mutant 前因找不到“至少两边锚点”失败。将该 mutant 的正基线改为近邻消费者的单锚点多边图，再人为拆一边，保持“本可合并却多复制”的原始故障语义。

普通三行紧凑图仍会被一行局部候选拆分，说明它不是稳定的“必然单锚点”对照。故障基线进一步收窄为同一零入度根连接同一 `mux2` 的两个近邻端口；两个目标轴天然属于同一局部带，再人为复制根并拆一条边，避免测试依赖布局行距偶然值。

同一 mux 两端口仍因端口轴差略大于当前 row pitch 被合法拆分，不能作为稳定的生产正基线。最终 mutant 构造先把该图所有根边显式归到一个主锚点并删除其它物理锚点，再复制主锚点、拆出一条边；它只测试独立 Oracle 的冗余别名拒绝能力，不再依赖当前生产分区是否会自然保留单锚点。

局部行带后 19 号为 63 个可见交叉点、56 个 source 交叉点、112 个折点、67,751.822 px；1 个新副本经后续列优化产生净收益。其余候选中 5 个因简单 H-V-H 穿节点被拒。分区路由改为保留旧路线中位于新共享 trunk 与目标之间的航点，避免候选生成器能力低于既有障碍路由器。

保留旧航点后拒绝分布未变。根因是局部分区先在继承列独立验收，只有接受后才允许列后移；需要“拆分+后移”才能安全的候选被阶段边界提前丢弃。局部行带创建时现同时为每个锚点从规范列中选择全部目标之前的最迟可行列，再做 y/route/完整质量比较。

## 冻结范围

- 公开反例 A：一个零入度源连接本地与多个远距离消费带，单锚点会形成长纵向干线和更多交叉；期望仅在完整质量向量更优时使用任意数量的源显示副本。
- 公开反例 B：一个公共源必须留在早期层，而若干低复用零入度源只服务较晚层；低复用源应进入第二列或其它最迟可行层，不能与公共源强制共列。
- 统计合同：所有逻辑节点均报告入度、出度、直接下级数、源端口网数、显示锚点数和出边质量聚合；所有逻辑边均报告长度、折点、线段、跨线点、跨线事件、不同相交边数和同源端口分支数。
- 覆盖合同：先冻结特性、状态、故障和环境，再声明高风险二元/三元交互；每项必须映射公开/合成场景、直接 Oracle、故障注入和测试节点。

## 交付门

1. 两个新示例从源码和最终发行包均生成自包含 SVG，且自由源列/副本行为由坐标和逻辑身份直接断言。
2. 生产内部统计与独立最终布局 Oracle 对所有节点、边、源和总计逐项相等；交叉注入必须同时命中两边，不同相交边数保持对称。
3. 覆盖账本校验器接受完整账本，并拒绝删除特性覆盖、交叉覆盖、Oracle、故障角色或测试目标的变体。
4. 完整 pytest、项目 skill、源码隔离、冻结包、Linux 发行消费和远端资产复验全部绑定同一提交/制品。

## 下一步

无开放交付项；后续修改布局、源副本或重叠阈值时必须保留复杂语料、退化突变、组合变形和远端资产下载门。

## 14:53 统计实现阶段

专项 JSON 合同已通过用户根能力闭环校验器。生产内部报告已开始使用每边位集合累计不同相交边，并为所有逻辑节点（含叶节点）补齐入度、出度、直接下级、源端口网、显示锚点、分支、长度、折点和交叉聚合。实现审查发现逐源不同相交边初稿误用了按边数的二次遍历；在进入测试前改为位集合并集，避免把诊断功能变成规模瓶颈。

独立 `layout_quality` Oracle 使用自己的最终几何扫描线和位集合实现同一统计合同；它不读取生产统计缓存。下一步先用既有跨线样本和故障注入证明伙伴计数对称且能拒绝篡改，再扩展公开示例。

## 15:12 公开反例

沿用并加强公开样例 `19-dispersed-root-fanout` 作为“一个逻辑源服务上/下远距离消费带”的展示；新增 `23-middle-column-low-use-sources`：公共源因较长公共路径留在第一列，四个仅服务一个局部 mux 的零入度源因一层松弛进入第二列。两者都加入全量示例构建和发行包清单，后续由最终 x 坐标、别名身份、路线统计与包消费直接断言。

新样例源码生成耗时 338 ms；独立质量 Oracle 通过，公共源 rank/x 为 0/89.83，四个低复用源 rank/x 为 1/228.9，28 条边零交叉、8 个总折点。新增包内项目 skill 工具用于把内部逐节点/逐边统计输出为 Agent JSON；它不是产品子命令，也不改变用户 SVG。

首次统计专项复验为 3 PASS / 1 FAIL。失败不是实现差异，而是新增叶节点断言写成不存在的 `wide_clock_0`，实际冻结配置名为 `wide_clock_top_0`；错误可由 pytest 的 `KeyError` 复现。测试名改正后重新运行原四项，不把该轮计为通过。

原四项复验已 4/4 通过（0.51 s）。用户根 `agent-quality-workflow` 新增特性空间测试集专题和通用 JSON 校验器；同一无私人信息实现复制到发行包内项目导航 skill，以保证内网源码/压缩包能独立审计项目覆盖账本。下一步建立 drawclock 特性、风险交互与场景矩阵，并以删除 mutant 验证校验器。

在写覆盖矩阵前先补两个真实 fault owner：包内统计工具的结构验证函数必须拒绝缺叶节点与边总数漂移；自由源列测试把四个低复用源强制改回公共源 x 后，独立 rank/坐标断言必须拒绝。覆盖账本只引用已经存在并能失败的测试节点。

项目覆盖账本首版从产品入口、图/端口、分层/路由、SVG、规模、统计、发行和 skill 八个质量面独立列出 21 项特性；另声明 12 个不能被单项用例替代的高风险交互，并映射到 23 个成功、边界、故障、环境或发行场景。新增 validator 自测会删除一个必需角色、删除交互覆盖并伪造测试函数；任一 mutant 存活即阻断。

首次机器差集按预期阻断：`sparse-mux` 和 `frequency-table` 虽有 boundary 场景，却各缺独立 success 角色，导致账本测试 1 FAIL / 6 PASS。新增“完整器件示例含 mux”和“频率表一行一时钟”成功场景后重跑同一门；不通过删减 required role 来放行。

覆盖账本复验通过：21 项特性、12 项高风险交互、28 个场景全部闭合；含删除自由源故障场景、删除终端交互场景和伪造 pytest 节点三个 mutant，均被校验器拒绝。用户根三个专题 skill 首次在 Windows 默认 GBK 下校验时由 Python 解码失败；固定 `PYTHONUTF8=1` 后逐个复验，`clock-tree-layout`、`agent-quality-workflow`、`svg-artifact-production` 均通过，确认是校验工具的进程编码问题而不是 skill 内容放行。

新增专项门 8/8 通过（0.51 s）。首次全量门为 383 PASS / 1 FAIL / 5 SKIP（80.85 s）：发行包单元测试的合成项目夹仍只创建旧的六个示例，打包器新增读取 19、23 后按预期因文件不存在失败。同步测试夹清单加入两个公开示例后重跑原失败项和全量门，不修改打包器的缺文件硬失败行为。

原失败项复验 1/1 通过（0.87 s），第二轮全量 384 PASS / 5 环境 SKIP（80.24 s）。Agent 统计工具对 19、23、1024、2048、4096 分别报告 32.652 ms、7.568 ms、1.774 s、5.885 s、23.538 s；逐节点/逐边计数均与生成图闭合。视觉复核确认 23 的公共源位于第一列、四个低复用源位于第二列且 28 边零交叉；19 经全局排序把同源消费者聚成连续带，故保留一条长总线而非无收益副本，文档改为准确描述“按最终几何成本选择”。

用户追加“无缘无故多折点但不一定跨线”的覆盖要求。现有故障只把多折点与跨线一起注入，无法单独证明折点 Oracle。新增净空通道 fault：保持端点不变，在水平直线上加入四折矩形凸起，且明确断言交叉列表为空；独立单调路线 dominance 必须仅凭少折点/短路径拒绝。覆盖账本增加独立 fault 场景，场景数由 28 增为 29，待专项与全量原门复验。

新增折点专项与账本复验 3/3 通过（0.52 s），全量门更新为 385 PASS / 5 环境 SKIP（80.05 s）。首次 Windows 一条龙冻结构建成功（100.697 s，8,135,544 B exe，37,140,494 B zip），但审计发现 frozen smoke 仍未消费新增的 19/23，源码消费门也只校验 skill 文件存在、未实际运行包内统计工具。先加这两层直接断言后重新 clean pack；旧 zip 不作为本轮有效制品。

从首次 zip 全新目录解压后，新版消费门通过：frozen 真实绘制 19/23 并断言中间源列，源码在 `-I -S` 下运行包内统计工具并检查 29 节点、28 边和全部关键边字段（合计 30.284 s）。随后专项测试发现合成 source-manifest 夹具尚未创建新增的统计脚本，1 FAIL / 8 PASS；这证明缺文件硬门生效。同步夹具后重跑原专项。覆盖校验器一次命令误写为不存在的 `--root`，属于调用错误，不计通过；按帮助改用 `--project-root` 重跑。

夹具与正确覆盖命令复验通过：21 特性、12 交互、29 场景，专项 9/9 PASS（2.39 s）。随后全量出现 4096 性能门 1 FAIL：33.305 s 超过 30 s；独立复验仍为 31.714 s，不能归为偶发。分析确认逐边/逐节点伙伴集合属于 Agent 诊断，而产品 SVG 入口不消费该报告；将几何统计改为内部显式 opt-in，Agent 工具和两项统计 Oracle 开启，正常生成不物化诊断集合。此为职责分离，不放宽 30 s 门，待原 4096、统计相等和全量复验。

职责分离后 4096 原门连续两次通过（pytest 内 28.80 s、27.32 s），统计相等与多源副本专项同时通过；最终全量 385 PASS / 5 浏览器环境 SKIP（97.63 s），30 s 上限未改。随后 Windows clean pack 通过（98.399 s）；最终 exe 8,135,544 B，SHA-256 `AF4A4F97729CBE3F6734CCF4A98FF75C5DE96B5EE3A60805E00EA55E5B437974`，zip 37,140,540 B，SHA-256 `D7AC0CD83FE7C3B2BAE96D43A3811DB59A65E6BC04E45A52F660573EC8A55650`。新目录解压后的 frozen 与 `-I -S` source/统计工具消费门均通过（29.834 s），准备上传与远端 Linux Release 门。

交付前按用户追加要求再次审计用户根知识面：原自由源专题不足以单独承担“自由布局整体”导航。新增通用自由布局专题，分离 layering / ordering / coordinate assignment / routing 四个所有者，汇总 ELK、yFiles、Graphviz、libavoid、Dagre、React Flow 与有界精确 Oracle 的角色边界，冻结十类反例族、组合覆盖、逐元素观测和兼容性禁令；明确不得按器件名、样例 ID、节点数阈值或后校准修补。用户根 `clock-tree-layout` UTF-8 quick validation 通过。

提交 `1e9e2f4` 已推送，远端 Release run `33606027910` 成功，v1.0.0 peeled tag 与提交一致。远端资产 51,931,379 B，API 与本机下载 SHA-256 均为 `57B95F8F337A4D64736FFCC0848AA3FA427E1BAB7DA61200B1B43CE4DCE4B168`，归档含 19/23、统计工具和源码。用户质疑本地 Docker 后复核：本机仅执行一次 `docker version` 只读探测且 engine 返回 HTTP 500，没有启动容器；Linux 构建/运行来自 GitHub 托管 runner。用户根 frozen gate 新增通用规则：本机不以 Docker 为交付依赖，异平台由平台原生/托管 CI 运行，本机只做异平台资产结构证据并明确边界。

## 完成结论

自由源后移、源显示副本、逐节点/逐边统计、无交叉多折点故障、特性/交互覆盖账本、两个公开示例、本地 Windows frozen/source 消费、远端 Linux frozen/source/librsvg 消费、tag 与远端资产摘要均已闭合。未把 NIST PDF 下载超时折算为成功；采用 NIST 官方网页正文作为降级证据，PDF 离线快照仍是资料完整性缺口，不影响已实现的项目机器门。

## 16:53 用户撤销 19 号示例验收

用户指出现有 19 号示例含 `wide_root`、`local_root` 两个不同源，不能展示“配置里只有一个逻辑源，而同名源图案因远距离下级被自动例化多次”。该意见成立，上一轮对公开反例 A 的完成声明撤销，记录重新置为 active。

不改坐标的结构性复现已找到：唯一零入度 `shared_source` 直接连接两个近端 gate 和一个展开十行下级的共享 hub；自然排序使 hub 成为远端直接下级。现有设施分区自动生成两个同逻辑名显示锚点，并使曼哈顿走线减少约 466 px。下一步把该自然场景替换为 19 号示例，并增加源码、SVG 和冻结包身份/使用/收益门禁。

## 22:01 复杂多源语料与独立 Oracle

将 19 号样例从旧的 12 行固定名称比对升级为独立拓扑特征推导：统计根、终端祖先签名、PAD 端口占用、PAD+root 混合 mux、source 对共祖覆盖、直接支路复用次数与非周期行间隔；另加退化突变测试，确认旧浅层语料会被拒绝。首次执行为 1 FAIL / 1 PASS（3.6 s）：28 种 source 对只覆盖 23 种，门禁按设计失败；不放宽阈值，调整语料补足组合后重跑。

把最后一个 PAD 混合支路的附加根从已重复贡献的 `source_d` 改为 `source_h`，用拓扑组合补足此前缺失的根对，而非降低 Oracle 下限；保持根数、行数、深度分布和端口占用不变。

第二轮仍为 1 FAIL / 1 PASS（3.9 s），但拓扑 Oracle 已全部通过；失败来自旧断言把显示顶点数强制等于逻辑节点数。复杂离散消费按设计产生 22 个 source 显示锚点，因此改为断言逻辑身份全集无增减，并单独要求存在源副本，而不是禁止合法的多锚点显示。

修正后拓扑与退化突变专项 2/2 通过（3.5 s；pytest 内 0.66 s）。下一层把同类但独立实现的结构 Oracle 放入最终冻结可执行文件消费门，避免源码测试通过而发行样例仍旧或缺失。

19 号 SVG 单独生成耗时 2.1 s；Agent 统计生成耗时 190.497 ms，190 条逻辑边、245 个交叉对事件、100 个不同交叉点、118 个折点、总曼哈顿长度 74231.207 px，完整质量门通过。冻结消费脚本已改为独立推导 8 根、48 终端、8 PAD、端口占用、24 祖先签名、25 根对和 8 个混合 mux，并核对 SVG 逻辑身份与源显示锚点；直接对源码 SVG 复验通过（1.8 s）。

增加同一复杂语料的组合变形门：所有节点中性重命名、顶层 JSON 对象逆序、每个多输入 `source` 成员逆序后，结构覆盖摘要必须与原图相同，且重新布局仍须通过端口、身份、副本与几何硬门。该门用于排除按实例名或输入枚举顺序缝补。

变形门首次执行 2 PASS / 1 FAIL（4.0 s；pytest 内 1.08 s）：变形后的拓扑摘要、190 边与逻辑身份均保持，但最终几何质量未通过，证明当前实现仍有隐藏的名称或枚举顺序敏感性。保留失败变形并提取硬失败，不通过删除变形或放宽门禁处理。

为失败断言增加硬失败诊断时，首次文本替换命中了同文件较早的同形断言；在执行测试前已识别，恢复原断言并只给变形门增加诊断消息。

硬失败定位为 `e170` 与 `e102/e125/e143/e163` 四组异源纵线共线。独立 Oracle 抓到约 9 px 的短重叠，而生产 `assess_layout` 只把长度至少 10 px 的重叠计入候选否决，因此源副本候选被错误接受；这也是此前质量闭环漏检的精确原因。结构性修复是统一生产候选评估与最终 Oracle 的“任何正长度异源共线均失败”语义，不按节点名或该样例坐标修补。

生产评估的扫描线活跃区间与重叠阈值已统一为 `1e-6` 几何容差：同网总线仍允许共享，异网任何可见正长度共线都会阻止候选。待变形原失败项及全部路由回归验证。

变形原失败项修复后 1/1 通过（3.5 s；pytest 内 0.60 s）。该变形不再接受产生短异源共线的副本候选，最终独立硬门通过。进入覆盖账本、公开说明和完整回归同步。

公开 README、项目设计记录、变更记录和覆盖账本已同步新语义；账本新增成功变形门与旧 12 行浅层语料 fault 角色，不再引用旧测试名或“禁止显示副本”的错误 Oracle。

包内布局 skill 的硬门已明确为“异网任何超过浮点容差的正长度共线均失败”，并登记复杂多源组合、量化 Oracle、退化突变和三种组合变形，避免以后再次引入 10 px 盲区。

相关回归 70/70 通过（64.75 s）。按新生产阈值重新生成 19 SVG 后，全量门为 396 PASS / 1 FAIL / 5 环境 SKIP（74.48 s）；旧 `05-dense-cross-root` 的 `hard_pass` 失败，证明该图原有短异网共线曾被 10 px 阈值掩盖。严格标准不回退，继续定位通用路由候选与该硬门的差异。

失败图四个布局候选均含 1～3 个短异网重叠。根因是旧正交路由候选评分也只在重叠长度达到一个完整 grid 时惩罚，与最终硬门不一致；现统一为 `1e-6` 正长度容差，使候选阶段先消除短重叠，而不是到最终评估才失败。

原失败项与复杂变形项 2/2 通过（3.9 s；pytest 内 0.86 s）；最终第二轮全量 397 PASS / 5 目标环境 SKIP（74.16 s）。SKIP 是浏览器/异平台环境专项，后续由发行 CI 的 Linux/librsvg 门承担，不折算为本地通过。开始 Windows clean pack、解压 frozen/source 消费和制品哈希。

Windows clean pack 完成（约 75 s），产出 `drawclock.exe` 与 `drawclock-1.0.0-windows.zip`。首次解压消费命令错误地假设发布包包含 `tools/`，两个 Python 驱动均报 `can't open ...\\tools\\run_*`；包清单显示其只含产品、源码、runtime、skills、示例和文档，故改由仓库独立门禁驱动解压包，并补一条随机 cwd 下只引用包内 JSON/库的直接 frozen 命令。该失败不记为通过。

正确的解压消费门全部通过（约 31 s）：仓库独立驱动运行解压后的 frozen 全流程；`run_source_release` 在新 venv、`-I -S` 路径下通过；随机 cwd 直接以包内 19 JSON、包内器件目录和解压后的 exe 生成 SVG；独立冻结 Oracle 复核复杂特征通过。Windows exe 为 8,138,392 B，SHA-256 `EC8E9D32E59261C2DDC1B01A273B61931294CE137FA47E75A2FA8E8269B776B3`；zip 为 37,147,804 B，SHA-256 `E1B25E5639B2FEFED8D84375323CE201BAEEFBE22AF286E9CBBB921369A50824`。

可复用教训已同步到用户根布局与 Oracle skill：删除“至少 10 px”盲区，要求生产候选、阶段接受与最终 Oracle 使用同一量纲/容差，并组合执行重命名、容器逆序和成员逆序；两个 skill 的 `quick_validate` 均通过（2.0 s），内容不含项目路径或私人信息。

功能提交 `9a58dc0` 已推送；Release run `33641432228` 的 Ubuntu 16.04 构建、frozen/source 解压消费、librsvg 与 publish 均成功，tag peeled 到该提交，初次远端资产为 51,937,802 B、API SHA-256 `c351bc2ca41f73320c9c2677bd52020827ecbce83bd88cd169e0f92052a20c62`。发布后审计发现 workflow 仍只消费 publish 前 artifact，缺少“从 Release URL 重新下载后运行”的第六层门；新增 publish 后下载、哈希、全新目录解压、frozen/source smoke，需再次推送并值守，旧 run 不作为最终闭环。

本机未安装 actionlint，按官方 GitHub Release API 获取当前 v1.7.12；首次把架构名猜作 `x86_64` 未找到资产，读取资产清单后改用官方实际命名 `windows_amd64`，下载成功且 workflow 校验零诊断通过（获取与校验约 58 s）。Release/skill 相关回归 9/9 通过（5.5 s；pytest 内 2.51 s）。未使用本机 Docker。

## 最终闭环

远端下载门提交 `6d47432` 的 Release run `33642389107` 全部成功；新增 `Download and smoke published release asset` step 明确为 success。当时的 v1.0.0 资产 51,938,092 B，Release API 与本机重新下载 SHA-256 均为 `c3cb3bad2c2e72642cf66c882c6875c5fc872fc87de621c594d5c243cdad0914`；归档含 19 JSON、项目 skills、完整源码和可执行位。工作记录自身提交会再次移动滚动 tag，因此不在受版本控制的记录中伪造一个会自我失效的“最终 SHA”；最终 main/tag/run/资产摘要以提交完成后的外部 Release API 收据为准。本轮已解决的工具失败均有替代闭环：无 `gh` 时使用官方 API；web 安全策略拒绝 API URL 后使用有界 PowerShell API；actionlint 资产名猜错后读取官方清单；首次包内 smoke 驱动路径假设错误后改用仓库独立驱动和包内输入直测。无未解决的自主学习、下载、权限、依赖或服务缺口。

用户随后撤销 23 号示例的充分性：旧配置给每个低复用源增加一个 local gate，只能演示从第 0 层后移到第 1 层，且故障门只是改 x 坐标，没有完整重排重路由，不能证明交叉、折点与长度收益。新的自然反例已在内存实验闭合：公共源经过三层共享链，再分别进入八个公共支路和 mux；八个低复用根直接进入对应 mux。无主观列约束时低复用根位于第 4 层、mux 位于第 5 层，结果为 0 交叉；把所有根通过已有 `layout_column` 合法强制到第 0 层后完整重排重路由，出现 7 个交叉、折点由 14 增至 16、总曼哈顿长度由约 8,865 px 增至约 13,620 px。该对照将替换弱坐标故障门。

19/23 的确定性生成器已改为上述自然拓扑；独立质量 Oracle 开始增加自己的拓扑排序与最迟可行层计算，不调用生产 `_ranks`，用于识别未受 `layout_column` 约束却仍落在前序层左侧的零入度节点。

独立根层 Oracle 的几何判据已落到最终可见包围盒中心轴：对有正松弛且无用户列约束的零入度节点，若其轴仍不在紧邻前序规范层之后，则形成 `avoidable-root-layer` 硬失败并输出节点、期望层、实际轴与前序层边界。

测试合同改为三把直接证据：自然 23 必须让八个低复用根位于第 4 层且在公共源与第 5 层 mux 之间；完整强制首列重排重路由必须同时出现更多交叉、折点和长度并被独立根层 Oracle 拒绝；任意重命名、逆序输入和 mux 端口对象逆序后仍保持同一层级与硬门结果。

发行消费门同步改为解析最终 SVG：八个低复用源必须共列，且该列严格位于公共源与八个 mux 的共列之间。项目覆盖账本把旧“坐标强改” fault 替换为完整重排重路由的三指标劣化与独立根层拒绝。

跨项目结论已去重写入用户根自由布局专题：自由源层级应视为加权分层而不是“第一/第二列”特例；故障对照必须完整重跑排序、坐标和路由；独立 Oracle 从最终几何检查最迟可行层。来源专题补记 React Flow、yFiles、OGDF、Graphviz、ELK 的角色与本轮 Find Skills 部分超时。

首轮直接门 6/6 通过（0.66 s）。19 号为 28 个逻辑节点、29 个显示节点、1 个同名源副本，生成约 30 ms；23 号为 44 个节点、无显示副本，生成约 17 ms。自然层级、完整首列反事实、重命名/逆序不变性及逐元素统计工具均通过。

Agent 逐元素统计复验：19 有 28 边、0 交叉、22 折点、总曼哈顿长度 9,016.465 px，`shared_source` 有 2 个显示锚点；23 有 43 边、0 交叉、14 折点、总长度 8,864.876 px。统计临时 JSON 不纳入仓库。

首次全量为 384 PASS / 3 FAIL / 5 SKIP（75.74 s）。三个失败均为新 Oracle 误报：属性语料中的公共根分别有 7、8、6 条出边；生产 merge cohort 为重复汇聚对齐增加约束层并把这些根保持在第 0 层，而简化独立 ALAP 未建模该约束，错误给出第 1/1/2 层。硬门收敛为可独立证明的低复用结构：零入度、无列约束且恰有一个直接下级；高扇出根继续由完整几何质量向量判断。

原三项失败加 23 正反门复验 14/14 通过（1.31 s）。用户根与项目能力合同同步区分：独立最终轴硬门只承诺单下级低复用根；一般多下级根仍由生产加权分层、重命名属性语料和完整几何质量向量覆盖。

修正后的第二轮全量 387 PASS / 5 环境 SKIP（74.18 s）。全部属性语料、示例、端口、路由、统计、覆盖与发行单元门均通过；跳过项仍为缺少对应外部浏览器/librsvg 环境的既有条件门，不折算为通过。

视觉复核显示：19 号 SVG 中同一逻辑 `shared_source` 的两个显示锚点分别服务近端和远端消费带；23 号 SVG 中八个低复用根全部位于公共源与 mux 之间，未见裁切或跨线。发行前审计发现源码包消费门仍把旧 23 号规模写死为 29 节点/28 边；改为从包内同一 JSON 动态计算节点与输入边数，使门禁检查语义覆盖而不绑定示例版本。下一步从 clean Windows 构建开始复验冻结入口、全新目录解压消费和远端发行。

首轮 clean Windows 包生成成功（约 78 s；exe 8,135,930 B，zip 37,140,901 B），但全新目录冻结消费按预期阻断：19 号门禁把“未被其他节点引用”的叶节点误认成根，报告 12 个时钟而非 `shared_source`。根因是辅助 Oracle 的集合方向写反；源码测试使用独立入度算法，不能覆盖冻结驱动器本身。修复为直接从每个节点是否声明上游 `source` 判断零入度，并播种最小单源/单终端 SVG 单测；旧实现会稳定失败。该 zip 作废，必须重新 clean 构建。

第二轮 clean 包（约 75 s）中冻结可执行文件完整工作流通过；后续 `python -I -S src` 已生成 SVG，但包内统计门失败。根因是去除 23 号规模硬编码时误把连接字段写成不存在的 `inputs`，把期望边数算成 0。修复为共享同一结构合同的独立 `_edge_count`：字符串 `source` 计一边，端口字典按项计边，无 `source` 计零；新增三种情况的直接单测。第二个 zip 同样作废并 clean 重建。

第三轮 clean Windows 包约 76 s 完成；全新临时目录消费约 42 s，冻结完整示例、包内七项 skill、`python -I -S src` 和包内逐节点/逐边统计全部通过。最终 exe 8,136,751 B，SHA-256 `D6EA1C28DBD81F11159D20A027A809FA537594A3B5B06205703ED1E68E3CED7B`；zip 37,142,136 B，SHA-256 `36F281DAC13912BC231ADDFEA470BD56CD47EE1BBA60C0E26CD9C0C5D3C7FA85`。最终全量 388 PASS / 5 环境 SKIP（78.49 s）；覆盖账本 21 特性、12 交互、29 场景，项目七项 skill、用户根专题、compileall 与 diff 检查均通过。用户根评估账本已记录自由根分层与“Oracle 也必须被故障注入”的跨项目经验，进入上传和远端 Linux 发行消费。

功能提交 `ba46ce7888127584b2fc12630bfd86d04b60c5f9` 已推送；滚动 `v1.0.0` peeled tag 与该提交一致，Release 页面声明托管 Linux 门已执行解压后的冻结程序、无依赖源码和 GNOME librsvg。远端下载资产 51,930,147 B，SHA-256 `FB7F06FC72B9B5771F8743E89DE4E3261776FD48DD1D5A0DF20C38766FEB9FCB`；本机只做下载哈希与归档清单审计，确认 19/23、源码、runtime、项目 skill 与覆盖合同存在，未使用本机 Docker。自由源布局、独立观测、测试覆盖和发行目标至此完成。

## 20:49 用户重新定义 19 号示例

旧 19 的“单个逻辑源显示为多个锚点”不符合新的示例语义，完成声明再次撤销。19 必须同时展示：若干独立 source 分别覆盖连续行；若干独立 source 交错覆盖行；若干 source 先进入同一个多输入 pad，再由 pad 输出服务下游行。旧单源副本能力迁移到独立编号，避免两个正交特性共用一个示例与 Oracle。旧 `pad` 的零输入单输出合同保持不变；新增多输入 pad 器件必须由端口拓扑通用驱动，禁止在布局器中按器件名加分支。

实现采用独立 `pad3` 文件与三入一出端口合同；旧单源副本场景迁为 24。19 的十二个终端行分别由一个连续行源、两个交错行源以及一个三源汇聚 pad 输出驱动，生产布局未增加器件名或样例名判断。

首次生成：器件库与 JSON 构建完成，全部 24 个 SVG 示例总耗时 30.8 s；19 为 43 节点/12 clocks，生成 9.7 ms、写出 6.7 ms。端口解析独立检查得到 `pad3 inputs={'in0':'0','in1':'1','in2':'2'}`、`outputs={'out':'C'}`。一次 pytest 调用因直接使用 `pytest` 时项目根未进入 `sys.path` 而收集失败；这是调用环境错误，改用项目既有 Python 模块入口复验，不计通过。

改用明确 `PYTHONPATH` 的模块入口后，器件、19/24 拓扑、冻结辅助 Oracle 与发行清单专项 66/66 通过（2.65 s）。Agent 逐元素统计显示 19 有 39 条边、0 交叉、16 折点、总曼哈顿长度 4,507.882 px；所有 12 条 gate→div 与 div→clock 行内边均为水平直线。布局会把交错编号的 A/B 消费行各自聚簇以消除交叉，但独立祖先 Oracle 仍验证输入拓扑的 A/B/A/B/A/B 行归属；这是全局交叉优化而不是篡改连接。

综合布局、可见端口、静态 SVG、逐元素统计、覆盖账本、skills、发行清单和 CLI 门 255/255 通过（73.67 s）。`git diff --check` 与 `compileall` 通过；项目设计笔记和 changelog 已同步新旧 pad 合同与 19/24 的职责分离。下一步执行 clean Windows 打包及全新目录冻结/源码消费。

clean Windows 打包约 93 s 完成，EXE 8,137,490 B，ZIP 37,145,385 B，ZIP SHA-256 `E451BB4E6304DDC558430DB1715FC5F87EAB0EF5569F825474D0D3305EDF797B`。全新目录解压后的 frozen 完整工作流和 `python -I -S` 源码消费通过。最终全量 395 PASS / 5 headless-browser 环境 SKIP（77.42 s）；跳过未折算为通过。

功能提交 `3c6dae1e51633865840fe56319bdcabe97a6b4f9` 已推送；Release run `33634127815` 的 Linux 与 publish job 均成功，`v1.0.0` peeled tag 与提交一致。远端资产 51,936,995 B，API 与本机实际下载 SHA-256 均为 `7D93E5E6AA30C77288A298C8C2F275A37F2E09FA94FCDB57BC57EBC5B67306EE`，归档含 `pad3.xml`、19、24、源码入口与 manifest。远端下载耗时 6 min 41 s；本机未使用 Docker。

## 完成结论（19 多源重设计）

19 现在以六个独立 source 覆盖连续行、交错编号行和三源经 `pad3` 汇聚后扇出；24 保留原单逻辑源显示副本。拓扑、端口、逐边质量、静态 SVG、本地冻结/源码包、托管 Linux 发行与远端资产完整性均闭合，生产布局没有新增器件名或样例名特判。

## 21:42 用户撤销小规模 19 验收

六根十二行、固定 A/B 交替只覆盖一个周期模式，不能代表大量 source 以不定间隔交错；旧完成声明撤销。新冻结范围为 8 个逻辑源、48 个终端：24 个非周期单源直连行，8 个根集合重叠的 `pad3`（同时覆盖三输入全连和两输入稀疏），16 个 pad 扇出行，以及 8 个 pad 输出再与另一个 source 经 mux 汇聚的混合行。支路深度变化，源复用度、行间隔和 pad 扇出数均不相同。

生成器已按冻结矩阵实现：8 个根的 pad 根集合有意部分重叠，两个 pad 分别留空不同输入端口；直连序列不是周期轮换，pad 扇出为 1/3/2/4/1/2/1/2，混合行分别把每个 pad 与另一个直接源再次汇聚。终端链按行号结构性改变 gate/div/cell 深度，不写坐标或布局提示。

## 04:20 自由 source 设施选址与精确统计收敛

复杂 19 已扩展为 190 条可见边，并冻结旧版本几何基线：100 个不同交叉坐标、93 个 source 诱发交叉坐标、118 个拐点、74,231.207 px 曼哈顿长度。生产统计与独立 O(E²) Oracle 现在分别核对“边对交叉事件”“不同相交边对”“不同交叉坐标”和逐边相交伙伴，避免把三个不同量纲互相覆盖。

通用布局改为显示设施问题：零入度逻辑源可按消费行带形成多个显示锚点；每个锚点独立搜索保持因果方向的可行列与最近全局可行行，移动后从精确端口重新生成正交路线。列、行、路线按硬几何、source 交叉、全图交叉、拐点、长度、面积的字典序接受；面积仅作末级目标。显式 `layout_column` 仍保持用户约束，非源节点不能复制，逻辑统计不因显示锚点增多而重复。

最终 19 为 20 次边对交叉事件、17 对不同相交边、15 个不同交叉坐标、8 个 source 诱发交叉坐标、86 个拐点、32,740.638 px 曼哈顿长度。相对冻结基线，交叉坐标下降 85%，source 交叉下降约 91%，拐点下降约 27%，长度下降约 56%。禁用局部设施、强制继承早列等完整反事实均显著劣化，并被独立门拒绝；重命名、节点逆序、端口对象逆序和 12 个属性语料种子保持量纲不变。

性能优化不改变质量词典序：候选仅复制可变几何、缓存标签宽度、复用阶段 assessment，并用按轴排序的反向空间索引只重查可能命中的可见盒。4096 级复杂压力由历史约 65.08 s 降至 23.85 s；最终全量为 400 PASS / 5 环境条件 SKIP，耗时 107.13 s。SKIP 仍只由目标浏览器/librsvg/异平台条件控制，不折算为通过；发行阶段须由托管 Linux 与远端下载消费门补齐。

发行前重跑在整套测试负载下捕获到 4096 级 30.526 s，超过 30 s 门限 0.526 s；该轮 399 PASS / 1 FAIL / 5 SKIP，耗时 120.082 s，不计通过且不放宽门限。cProfile 显示同一候选路线的障碍命中被可见图扩展、trunk 过滤和最终评分重复查询：21,261 次入口对应约 11,526 个候选，空间桶集合更新约 1,244 万次。下一轮优化限定为缓存同一 edge/points 的不可变命中集，不改变候选、评分或接受顺序。

缓存实现后，4096 硬门连续三次为 21.25、21.15、20.93 s，三轮均通过；端到端 pytest 进程分别为 22.684、22.450、22.244 s。缓存键包含完整逻辑边 key 与完整候选点列，值为不可变障碍集合，因此没有合并不同端口、不同路线或动态布局候选。

修复后从头执行 compileall、七项项目 skill、覆盖清单和全量 pytest：400 PASS / 5 条件 SKIP，pytest 94.53 s，总门 96.698 s。进入 clean Windows 构建、全新目录冻结/源码消费和远端 Linux/librsvg 门。

Windows clean pack 98.253 s 成功，产出 8,159,216 B EXE 与 37,176,305 B ZIP。首次准备系统临时目录消费的命令被宿主策略在进程创建前拒绝，原因是命令包含递归清理；没有执行、没有留下临时目录，也不记通过。替代方案是在仓库已忽略的 `work/` 下创建唯一目录，不做删除，再运行同一解压消费门。

替代消费门 35.679 s 通过：从全新解压根、隔离 PATH 运行冻结程序的完整公开功能样例；在新 venv 内以 `-I -S` 运行包内源码；校验包内七项 skill、manifest、逐节点/逐边统计、严格 JSON、多文件/目录器件库、任意输出后缀仍为 SVG。EXE SHA-256 为 `25B69DA7BBEC32893C925368CE2BE0D24CED622E3ACF948E060FC12A2E4CEE44`，ZIP SHA-256 为 `8BE7CB6035EE385BB26F2B990B1321B46ED611C6A7EF650D428F4A6283EEAC86`。
