# 发布项目 Skills

- status: active
- created: 2026-08-25 16:30 +08:00
- updated: 2026-08-25 17:45 +08:00
- scene: 发布项目 Skills

## 当前状态

- 已审计项目源码、用户文档、器件库、布局质量门、项目 skill 与用户根部相关 skill。
- 已检索公开 skill 候选、ELK、React Flow、Graphviz、JSON Schema、draw.io、自主分层布局与正交避障资料。
- 目标为在发行包根目录提供五个脱敏、单层渐进披露的项目 skill，并把完整性、隐私与包内可用性纳入机器门禁。
- 已创建 `skills/`：器件库、布局算法、JSON、成图设计、项目导航五个 skill，共 27 个渐进披露文档和一个标准库校验脚本。
- 布局资料综合了 ELK/Sugiyama 五阶段、Brandes–Köpf 及勘误、正交可见图与 hyperedge 路由；React Flow 明确定位为显示层参考。
- 器件目录和端口说明由当前 `drawclock.xml` 实际解析结果核对；公开 JSON 只保留 `kind`、可选 `source`、可选 `layout_column` 作为通用字段。
- 首轮项目校验准确发现校验器会匹配自身的 `TODO/TBD` 正则字面量；已把模式改写为等价但不自命中的形式。
- 通用 quick validator 在 Windows 中文代码页下读取 UTF-8 中文失败，错误为 `UnicodeDecodeError: gbk`；这是验证工具的环境编码问题，下一轮用 Python UTF-8 模式重跑，不修改 skill 为英文或本地编码。
- 项目校验器和五个通用 quick validation 均通过；第二轮耗时 15.5 秒。
- `skills/` 已加入发行复制集合和 source manifest；冻结门与源码门都要求五个入口及校验器存在并执行包内校验。
- 新增三类故障注入：私人 Windows 用户路径、断开的 reference 链接、未从根 skill 链接的 reference 文件。
- 源码部署说明、设计笔记、变更记录和 Release 描述已统一说明 skill 是包内维护知识而非运行时依赖。
- 聚焦打包/skill 测试 8 项通过，pytest 2.19 秒；静态编译、项目 skill 校验和私人路径同模式检索通过，耗时 3.0 秒。
- 完整回归 321 项全部通过，pytest 128.50 秒、外层计时 130.048 秒；现有端口、布局、质量与压力门无回归。
- Windows 静态包构建完成，耗时 104.787 秒；ZIP 为 37,057,164 字节，SHA-256 `29465BDFFED55C43013DB5B9428F6CE8F3CC87480F66EA9F2E85F25DA2C29A9D`。
- 真实 ZIP 解压后，包内 skill 门 0.186 秒、冻结绘图门 14.594 秒、空虚拟环境 `-I -S` 源码门 14.018 秒，全部通过。
- 提交前代码自审补读中文 Markdown 体裁、删句、表达、版式和禁用词规则；待压缩 skill 中的结构自述、空泛术语和目录职责表。
- 文档自审已将连接结构、同源多路连接等具体表述替代空泛术语，删除“先…再…”结构句，并把目录职责表改为带行尾注释的目录树。
- 第二轮检索发现 3 处残留顺序承接句，已改成条件或阶段陈述；`git diff --check` 无空白错误。
- 自审后五个 quick validation、项目校验器和 8 项聚焦测试再次通过，聚焦 pytest 3.58 秒。
- 最终 ZIP 增量重组 24.706 秒；解压后 skill 门 0.191 秒、冻结门 14.446 秒、源码门 13.123 秒，全部通过。
- 最终 ZIP 为 37,056,986 字节，SHA-256 `828509A37ECCC7E6BE16122EF79B11C1218033B7E2AEF70538FCB900C8491401`。
- 首次暂存门通过范围与敏感信息检查，但 `git diff --cached --check` 发现 8 个新 Markdown 文件末尾多空行；已返回编辑阶段修正。
- 格式修正后项目 skill 校验、8 项聚焦测试和 diff 空白检查通过；pytest 2.25 秒。
- 最终第二次重组 23.560 秒；解压包冻结门 14.317 秒、源码门 12.777 秒，均通过。
- 当前待发布 ZIP 为 37,056,973 字节，SHA-256 `E070B96393EE56D73A775F4E7FDE14F20D7B976196E7D2C23F57123F20434DEF`。

## 下一步

- 重新暂存，提交、发布并下载远端资产复验。
