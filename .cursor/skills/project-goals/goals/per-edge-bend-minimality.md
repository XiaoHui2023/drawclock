# 逐边最少拐点证明

## 状态

- status: done
- owner: agent
- updated: 2026-08-14

## 期望结果

- `sel` 输入 1 的连接在无障碍直线可行时不保留折线。
- 每条非共享普通边都与由当前器件、端口、文字范围和已用通道推导的更简路线比较。
- 门禁可以拒绝两拐点但可直连的路线，并覆盖镜像、换名、换尺寸和连接顺序变化。
- 512 至 4096 时钟压力配置保持既有正确性与耗时范围。

## 当前证据

- success: `div_b → sel[1]` 起点与终点均为 y=146，航点为 0；全图总拐点为 2，交叉、器件穿线和异网重合均为 0。
- success: 故障注入把完整独占链移开 20 px 后，schema 9 报告对应边为 `avoidable_exclusive_chain_bend_edges` 并拒绝该图。
- success: 两种 mux 输入镜像、实例换名、JSON 逆序和 83×91 自定义 gate 器件库测试通过。
- success: 12 个性质种子通过；1024/2048/4096 生成分别为 2.158/6.718/25.272 秒，新增整链移动数均为 0。
- success: 最终 298 项源码测试通过；Windows 发布压缩包从新目录解压后完成全部冻结工作流。
- success: GitHub Actions Release `31785600016` 成功；`v1.0.0` 指向 `f348619`，下载的 Linux 包 SHA-256 为 `7c7f58d798e24d8b62b364e86de0842d0a5b70618b6f60263d6ea0dbad7cb4e8`，隔离容器冻结工作流通过。
- failure: 用户在 `20-asymmetric-merge-route-bulge.svg` 看到 `sel` 输入 1 仍有可消除拐点。
- failure: 现有完整候选检查主要覆盖四拐点异常；两拐点路线只由部分结构规则间接检查。
- failure: 上一轮整图报告总拐点下降，但没有为每条边给出最少拐点证明，整体汇总掩盖了单边失败。

## 已尝试

- 2026-08-14: 整组移动 `gate_a_tap` 独占上游链，解决长距末端折线；没有覆盖相邻 mux 输入的普通两拐点路线。
- 2026-08-13: 对所有两拐点边做完整全图候选枚举，4096 时钟测试超过 124 秒；该方法已停用。

## 下一步

- 已收敛；新的生产拓扑反例按同一逐边结构门禁重新开启。

## 收敛条件

- 冻结反例、镜像与性质测试没有可避免拐点。
- 历史成功图的端口、穿越、异网重合、交叉、主干和边界指标不退化。
- 完整源码、压力配置、静态包与下载后 Release 检查通过。
