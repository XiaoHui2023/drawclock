# 用户反馈自然复现门禁

## 状态

- status: done
- owner: agent
- updated: 2026-09-04

## 期望结果

- 每条用户反馈在产品修复前均由冻结旧版本、公开 CLI、有效 JSON 和只读 Oracle 自然复现两次。
- 人工改图、故障注入、变异测试和自报状态不能解锁产品 owner。
- 未复现、入口未到达、证据缺失、问题仍开放或声明范围扩大时，机器门非零退出。
- commit、push、release 与完成声明消费当前 issue 集合和制品身份；不能复用旧 PASS。

## 当前证据

- failure: `FB-ROUTE-009` 已登记；`final-combined.svg` 的 `weave__public_gate→weave__select_04` 为 4 拐点、934.1805px、1 次交叉，旧固定坐标逐边 Oracle 无法表达根显示锚点拆分反事实。
- failure: `FB-ROOT-010` 已登记；`final-medium.svg` 的多个 `xtal_1` 单边显示锚点仍在纵干线左侧，单边跨 2–8 个异网伙伴，旧 Oracle 因逻辑总出度为 10 而跳过物理单边锚点。
- success: `FB-ROUTE-009` 与 `FB-ROOT-010` 已由当前公开 CLI 双跑转绿；通用修复以物理显示设施服务边集、连续可行列和消费者后缀走廊为自由度，不读取器件 kind、实例名或样例编号。
- success: 1024/2048/4096 仅布局门分别满足 5/10/30 秒预算；精确连续搜索与规范列搜索由拓扑工作压力选择，两层共用完整硬几何和质量向量验收。

- success: 60 个正常 JSON 组成搜索语料，正式证据语料采用 `case -> issue` 多对多关系；六条问题各有两次冻结公开入口自然红灯收据。
- success: 独立 Oracle 不导入生产模块，完整绑定节点、边和端口后统计交叉、重叠、规范折点、长度、同网环、根位置反事实和端口纵序。
- success: 121 节点综合 example 在当前版命中五项、在冻结 06c 命中含 PORT-006 的五项，跨版本直接观测并集覆盖六项。
- success: 用户根自然复现 runner/validator 的首轮自测能拒绝未复现、人工故障、伪造收据和入口失败。
- failure: 远端运行 `33720713223` 在六条问题仍为 `reported` 时发布了提交 `0a2f48b`；旧 CI 未调用反馈门，且发布 job 使用了 `always()`。
- success: 项目 release 检查器当前逐条报告六个未复现问题和两个流程事故并返回 1。
- success: 远端 run `33724488973` 在 feedback gate 失败，Linux build 与 publish 均 skipped；`v1.0.0` 仍指向 `0a2f48b`。
- success: 最终冻结基线批次 `20260903T142956Z-73e71644` 对 8 项问题各自然复现两次；当前修复批次 `20260903T155038Z-c00c8e7c` 对相同问题逐 case 双跑且全部不再命中。
- success: 最终全量回归 443 项通过、5 项按目标环境条件跳过；Windows ZIP 在全新目录中的冻结程序和标准库源码运行均通过。

## 发行结果

- commit `7aa0526` 的 Release run `33776334314` 三个 job 全部通过；`v1.0.0` 指向该提交，`v0.0.0` 仍指向 `8befc99`。
- 远端 Linux 资产为 17,237,791 bytes，SHA-256 `9409ac5115ad61fa05252a940f6c221cee763b243c35808b5262d798dcf55121`；CI 与独立下载消费均通过。

## 收敛条件

- 复现门的成功基线接受率与绕过负例拒绝率均为 100%。
- 对未复现问题修改 owner 路径的首次工具调用在副作用前被拒绝。
- 8 项布局问题均绑定冻结旧版红灯和当前公开入口绿灯；远端 Release 下载消费成功后，本目标进入 complete。
