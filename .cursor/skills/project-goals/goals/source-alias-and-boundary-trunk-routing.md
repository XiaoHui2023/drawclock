# 远距源显示设施与边界主干路由

## 状态

- status: active
- owner: agent
- updated: 2026-09-11

## 期望结果

- 零入度逻辑根的消费者形成远距、分离消费带，且单设施长主干会造成更长走线或更多交叉时，按完整几何收益使用多个同名显示设施；逻辑节点和逻辑边保持唯一。
- 公共根跨越多行时，主干保持在整体外侧或可见空闲边界，禁止在中部过早进入两行之间再向下贯穿其它网络；只有完整反事实证明内部通道更优时才允许进入。
- 新增要求或指标不得删除、覆盖或缩小既有要求。冲突必须形成显式冲突记录并阻断发布。
- 每张公开 SVG 都执行质量注册表的完整指标 exact-set；每个指标均有适用性回执、正例、边界例和 mutant。

## 当前证据

- success: 已冻结当前发布提交、登记两个独立问题 ID，并确认生产 `src/**` 尚未修改。
- failure: 用户再次报告远距双消费者仍未按几何收益拆分同名源设施，说明已有能力合同没有形成不可丢失的逐制品指标或其适用性判定存在逃逸。
- failure: 用户报告公共根可沿整体边界进入最下方，却在中部提前分叉进入行间并向下延伸，造成大量异网交叉/跨线；当前注册表没有独立的“过早内部入口被边界候选支配”指标。
- failure: 用户在 2026-09-11 复位该问题：公共 `from` 右侧存在密集分支，另一个消费者位于略偏右的右下方；当前产物仍可能让同源线路从密集分支中部进入后再向下，而不是比较从整个分支带下方绕行的全网络候选。旧 `FB-ROUTE-023` 的 PASS 因新反例撤销，必须以新的错列右下组合重新取得自然红灯。
- failure: 用户根布局资料同时保留“所有根固定首列”和“按完整几何收益决定首列/近端”的冲突规则，项目又把部分旧要求只保存在能力合同或未完成清单中；现有校验只验证文件结构和指标 exact-set，没有验证跨版本需求集合单调保留及跨文档语义冲突。
- success: reset 变异 seed-013 在发布基线自然命中 023；修复过程的第一、第二攻击 epoch 又分别在 seed-018、seed-019 捕获 002/023 与非正交相邻回退，均清零 streak 后重新修复。
- success: 最终第四 epoch 在同一源码上连续 24/24 clean；正式高风险合同 `20260911T053825Z-a3269815` 连续 R1–R7 clean，每个制品执行统一 25 指标，语义 exact-set 含外缘错位扇出。
- success: 当前 fix verification 批次 `20260911T054454Z-682e218b` failures=[]；用户根布局、质量与举一反三专题均已更新并通过 skill 校验。
- success: 最终不变源码全量测试 554/554；全部 26 张公开 SVG 新鲜生成并逐张执行统一 25 指标，26/26 PASS。seed-019 最终 SVG 独立 25/25 PASS，PNG 为 4060×4703、447364 bytes，稳定交付别名与源 SHA-256 一致。
- success: 当前暂存闭包 release gate 19/19；Windows PyInstaller 构建和全量 frozen feature smoke 通过，zip SHA-256=`25712860ea90371c431752a5410d25dfd7fba908ac3343fe826b736f2d8e65bc`，并已在全新解压目录再次消费通过。

## 已尝试

- 2026-09-09: 审计用户根与项目 Skill、目标和质量注册表，确认旧意图存在但归属分散且规则冲突；尚未修改生产布局 owner。

## 下一步

- 完成打包/冻结消费与 release gate，将新 fix/recursive 原始证据纳入 Git 闭包。
- 提交、推送、滚动发布并从公开 Release 重新下载校验哈希与消费；最终响应使用经路径门生成的稳定绝对路径交付图片。

## 收敛条件

- 两项用户反馈均有冻结旧版公开入口双跑红灯、当前双跑绿灯、相邻边界、mutant 和连续 7 轮异构 clean 收据。
- 项目必选需求/指标集合只能新增或经显式冲突决议变更；删除、改 ID、缩小适用范围、测试选择指标子集均被机器拒绝。
- 所有公开制品完整执行同一指标集合，全量测试、打包、全新消费、上传和 Release 闭环通过。
