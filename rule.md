# 画图规则

## 库图形

时钟树上的器件来自 **drawclock** 库。

注释、普通形状可以画在边上，不接到库器件。

## 实例名

除 `wire` 外，每个 `name` 不重复。

## 连线

一个输入只接一路上游。一个输出可以驱动多路下游。

## clock

`freq` 后缀 `k` / `m` / `g`，大小写不限。

## wire

多张 `.drawio` 里，同名 `wire` 是同一条线。

左端一路、右端可多路。一张图右端可以不接，由另一张图上的同名 `wire` 补上。
